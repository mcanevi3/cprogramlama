\documentclass[tikz,border=2mm]{standalone}

\usepackage[utf8]{inputenc}
\usepackage[turkish]{babel}
\usepackage[T1]{fontenc}
\usetikzlibrary{circuits.logic.US, positioning}

\begin{document}
\shorthandoff{=}
\begin{tikzpicture}[circuit logic US, every node/.style={font=\small}, 
  x=3cm, y=2cm]

  % Input nodes for A, B, and Carry-In
  \foreach \i in {0,1,2,3} {
    \node (a\i) at (0,-\i+0.2) {$A_\i$}; % A inputs
    \node (b\i) at (0,-\i-0.1) {$B_\i$}; % B inputs
  }
%   \node (cin) at (-1,-4.5) {$C_{in}$}; % Carry-In

  % Full adder blocks
  \foreach \i in {0,1,2,3} {
    \node [draw, rectangle, minimum width=2.5cm, minimum height=1.5cm, label={[yshift=-1cm]Toplayici $\i$}] (fa\i) at (1,-\i) {};
  }

  % Output nodes for Sum and Carry-Out
  \foreach \i in {0,1,2,3} {
    \node (sum\i) at (2,-\i) {$S_\i$}; % Sum outputs
  }
  \node (cout) at (2,-3.87) {$C_{out}$}; % Carry-Out

  % Draw connections
  \foreach \i in {0,1,2,3} {
    % Connect A and B to Full Adder inputs
    \draw (a\i.east) -- ++(right:0.5) -- (fa\i.west|-a\i.east);
    \draw (b\i.east) -- ++(right:0.5) -- (fa\i.west|-b\i.east);

    % Connect Sum output
    \draw (fa\i.east) -- ++(right:0.5) -- (sum\i.west);
    
    % Connect Carry-In and Carry-Out
    \ifnum\i=0
    %   \draw (cin.east) -- ++(right:1) -- (fa\i.north);
    \else
      \draw (fa\the\numexpr\i-1\relax.south) -- ++(down:0.25) -| (fa\i.north);
    \fi
  }
  % Final Carry-Out connection
  \draw (fa3.south) -- ++(down:0.5)  -- (cout.west);
\end{tikzpicture}
\end{document}
